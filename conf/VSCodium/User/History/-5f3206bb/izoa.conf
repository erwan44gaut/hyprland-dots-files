background {
    monitor =
    # path = /home/erwan/Downloads/iss068e054895_orig.png  # only png supported for now
    color = rgba(0, 0, 0, 1)
}

input-field {
    monitor =
    size = 150, 30
    outline_thickness = 3
    dots_center = false
    outer_color = rgb(151515)
    inner_color = rgb(200, 200, 200)
    font_color = rgb(10, 10, 10)
    fade_on_empty = true
    hide_input = false

    position = 0, 0
    halign = center
    valign = center
}

label {
    monitor =
    text = Hi there, $USER
    color = rgba(255, 255, 255, 1.0)
    font_size = 20
    font_family = Noto Sans

    position = 0, 60
    halign = center
    valign = center
}
